<resources>
    <style name="MainButton">
        <item name="android:backgroundTint">@color/primary</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:padding">20dp</item> <!-- plus grand padding -->
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">20sp</item> <!-- taille du texte -->
        <item name="android:minHeight">64dp</item> <!-- hauteur minimum du bouton -->
        <item name="android:minWidth">200dp</item> <!-- largeur minimum du bouton -->
    </style>
</resources>
